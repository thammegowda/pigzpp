[package]
name = "pigzpp"
version = "1.1.0"
edition = "2021"
description = "Rust bindings for pigzpp (parallel gzip + PNG) via its C ABI"
license = "Zlib"
links = "pigzppc"
build = "build.rs"

[lib]
name = "pigzpp"
path = "src/lib.rs"
